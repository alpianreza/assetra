import { useNavigate } from 'react-router-dom';
import { Bell, CheckCheck, ChevronRight, Clock3, TriangleAlert } from 'lucide-react';
import { Button, DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuTrigger } from '@/components/ui';
import { useMarkAllNotificationsRead, useMarkNotificationRead, useNotifications } from './hooks';
import type { AppNotification } from './api';

function relativeTime(value:string){const seconds=Math.floor((Date.now()-new Date(value).getTime())/1000);if(seconds<60)return'Baru saja';if(seconds<3600)return`${Math.floor(seconds/60)} menit lalu`;if(seconds<86400)return`${Math.floor(seconds/3600)} jam lalu`;return`${Math.floor(seconds/86400)} hari lalu`}
export function NotificationBell(){const navigate=useNavigate();const{data,refetch}=useNotifications(8);const markRead=useMarkNotificationRead();const markAll=useMarkAllNotificationsRead();const notifications=data?.data?.items??[];const unread=data?.data?.unreadCount??0;const openItem=async(item:AppNotification)=>{if(!item.read)await markRead.mutateAsync(item.id).catch(()=>undefined);navigate(item.href)};return<DropdownMenu onOpenChange={open=>open&&void refetch()}><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" className="relative" aria-label={`Notifikasi${unread?` (${unread} belum dibaca)`:''}`}><Bell className="h-4 w-4"/>{unread>0&&<span className="absolute right-0.5 top-0.5 flex min-h-4 min-w-4 items-center justify-center rounded-full bg-red-500 px-1 text-[9px] font-bold text-white">{unread>99?'99+':unread}</span>}</Button></DropdownMenuTrigger><DropdownMenuContent align="end" className="w-[min(92vw,380px)] p-0"><div className="flex items-center justify-between px-4 py-3"><DropdownMenuLabel className="p-0">Notifikasi</DropdownMenuLabel>{unread>0&&<button className="flex items-center gap-1 text-xs font-medium text-primary" onClick={()=>markAll.mutate()}><CheckCheck className="h-3.5 w-3.5"/>Tandai dibaca</button>}</div><DropdownMenuSeparator className="m-0"/><div className="max-h-[420px] overflow-y-auto">{!notifications.length?<div className="px-6 py-10 text-center"><Bell className="mx-auto h-8 w-8 text-muted-foreground/50"/><p className="mt-3 text-sm font-medium">Belum ada notifikasi</p><p className="mt-1 text-xs text-muted-foreground">Pengingat checklist akan muncul di sini.</p></div>:notifications.map(item=><DropdownMenuItem key={item.id} onSelect={()=>void openItem(item)} className="cursor-pointer items-start gap-3 rounded-none border-b px-4 py-3 last:border-0"><span className={`mt-0.5 flex h-9 w-9 shrink-0 items-center justify-center rounded-xl ${item.type==='late'?'bg-red-500/10 text-red-600':'bg-amber-500/10 text-amber-600'}`}>{item.type==='late'?<TriangleAlert className="h-4 w-4"/>:<Clock3 className="h-4 w-4"/>}</span><span className="min-w-0 flex-1"><span className="flex items-start gap-2"><span className="line-clamp-1 flex-1 text-sm font-semibold">{item.title}</span>{!item.read&&<span className="mt-1.5 h-2 w-2 rounded-full bg-primary"/>}</span><span className="mt-1 line-clamp-2 text-xs leading-5 text-muted-foreground">{item.message}</span><span className="mt-1 block text-[10px] text-muted-foreground">{relativeTime(item.updatedAt)}</span></span></DropdownMenuItem>)}</div><DropdownMenuSeparator className="m-0"/><button className="flex w-full items-center justify-center gap-1 px-4 py-3 text-xs font-semibold text-primary hover:bg-muted" onClick={()=>navigate('/notifications')}>Lihat semua notifikasi<ChevronRight className="h-3.5 w-3.5"/></button></DropdownMenuContent></DropdownMenu>}
