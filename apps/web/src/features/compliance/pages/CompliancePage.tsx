import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useComplianceOverview } from '../hooks';
import { useAuth } from '../../auth/useAuth';
import { Badge,Button,Card,CardContent,Input } from '@/components/ui';
import { EmptyState,Skeleton } from '@/components/shared/states';
import { Boxes,ChevronRight,ClipboardCheck,MapPin,QrCode,Search } from 'lucide-react';
export function CompliancePage(){const navigate=useNavigate();const{hasPermission}=useAuth();const canExecute=hasPermission('compliance.execute');const{data,isLoading}=useComplianceOverview();const[search,setSearch]=useState('');const inventories=data?.data??[];const filtered=inventories.filter((i:any)=>[i.assetCode,i.itemTypeName,i.areaName,...(i.templates??[]).map((t:any)=>t.name)].some(v=>v?.toLowerCase().includes(search.toLowerCase())));return <div className="space-y-6"><section className="rounded-3xl border border-primary/15 bg-gradient-to-br from-primary/15 via-card to-card p-6 sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.2em] text-primary"><ClipboardCheck className="h-4 w-4"/>Operasional</p><h1 className="mt-2 text-2xl font-bold">Pelaksanaan Checklist</h1><p className="mt-2 text-sm text-muted-foreground">Cari aset untuk mengisi checklist atau melihat hasil pemeriksaan.</p></div><Badge variant="secondary" className="w-fit px-3 py-1.5">{inventories.length} inventaris siap</Badge></div></section><div className="grid gap-4 sm:grid-cols-2"><Card><CardContent className="flex items-center gap-4 p-5"><Boxes className="h-8 w-8 text-primary"/><div><p className="text-xs text-muted-foreground">Inventaris dengan checklist</p><strong className="text-2xl">{inventories.length}</strong></div></CardContent></Card><Card><CardContent className="flex items-center gap-4 p-5"><QrCode className="h-8 w-8 text-cyan-500"/><div><p className="text-xs text-muted-foreground">Akses cepat</p><strong className="text-base">Scan QR atau pilih aset</strong></div></CardContent></Card></div><div className="relative max-w-xl"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><Input className="pl-9" placeholder="Cari nomor inventaris, jenis item, area..." value={search} onChange={e=>setSearch(e.target.value)}/></div>{isLoading?<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{[1,2,3,4,5,6].map(i=><Skeleton key={i} className="h-44"/>)}</div>:!filtered.length?<EmptyState title="Inventaris tidak ditemukan" description="Pastikan aset memiliki jenis item dan pertanyaan checklist."/>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map((i:any)=><Card key={i.id} className="group overflow-hidden transition hover:-translate-y-1 hover:shadow-lg"><CardContent className="p-5"><div className="flex items-start justify-between"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><ClipboardCheck className="h-5 w-5"/></span><Badge variant="outline">{i.templates?.length||0} template</Badge></div><h3 className="mt-4 font-mono text-sm font-bold text-primary">{i.assetCode}</h3><p className="mt-1 text-lg font-semibold">{i.itemTypeName||'Jenis item belum tersedia'}</p><p className="mt-2 flex items-center gap-1 text-sm text-muted-foreground"><MapPin className="h-4 w-4"/>{i.areaName||'Tanpa area'}</p><div className="mt-4 border-t pt-4"><Button className="w-full" variant={canExecute?'default':'outline'} onClick={()=>navigate(`/inventory/${i.id}`)}>{canExecute?'Buka Checklist':'Lihat Hasil'}<ChevronRight className="ml-2 h-4 w-4 transition-transform group-hover:translate-x-1"/></Button></div></CardContent></Card>)}</div>}</div>}
