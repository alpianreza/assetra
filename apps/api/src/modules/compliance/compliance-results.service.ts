import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../../database/prisma.service';
@Injectable()
export class ComplianceResultsService {
  constructor(private readonly prisma: PrismaService) {}
  async getResult(inventoryId:number,occurrenceId:number){const header=await this.prisma.checklistLog.findFirst({where:{id:occurrenceId,inventoryId,questionId:null},include:{template:true,session:true}});if(!header)throw new NotFoundException('Hasil checklist tidak ditemukan');const[inventory,answerLogs,checker]=await Promise.all([this.prisma.complianceInventory.findUnique({where:{id:inventoryId},include:{itemType:true,area:true}}),this.prisma.checklistLog.findMany({where:{inventoryId,templateId:header.templateId,periodKey:header.periodKey,sessionId:header.sessionId??null,questionId:{not:null}},orderBy:{id:'asc'}}),this.prisma.user.findUnique({where:{id:header.checkedById},select:{id:true,name:true}})]);if(!inventory)throw new NotFoundException('Inventaris tidak ditemukan');const questionIds=answerLogs.map(log=>log.questionId).filter((id):id is number=>id!==null);const[questions,evidences]=await Promise.all([questionIds.length?this.prisma.checklistQuestion.findMany({where:{id:{in:questionIds}},select:{id:true,questionText:true,sortOrder:true}}):[],answerLogs.length?this.prisma.evidence.findMany({where:{checklistLogId:{in:answerLogs.map(log=>log.id)}}}):[]]);const questionById=new Map(questions.map(q=>[q.id,q]));const evidenceByLog=new Map(evidences.map(e=>[e.checklistLogId,e]));const answers=answerLogs.map(log=>{const q=log.questionId?questionById.get(log.questionId):null;const evidence=evidenceByLog.get(log.id);return{id:log.id,questionId:log.questionId,questionText:q?.questionText??'Pertanyaan tidak tersedia',sortOrder:q?.sortOrder??Number.MAX_SAFE_INTEGER,status:log.status,remark:log.remark,photo:log.photo,evidence:evidence?{id:evidence.id,fileName:evidence.fileName,mimeType:evidence.mimeType,sizeBytes:evidence.sizeBytes,url:`/api/v1/compliance/evidence/${evidence.id}/file`}:null}}).sort((a,b)=>a.sortOrder-b.sortOrder);return{occurrenceId:header.id,inventory:{id:inventory.id,assetCode:inventory.assetCode,itemTypeName:inventory.itemType?.name??null,areaName:inventory.area?.name??null,specificArea:inventory.specificArea},template:{id:header.template.id,name:header.template.name},period:{key:header.periodKey,label:header.periodLabel??header.periodKey},session:header.session?{id:header.session.id,name:header.session.name}:null,checkedAt:header.checkDate,checkedBy:checker,status:header.status,summary:{total:answers.length,ok:answers.filter(a=>a.status==='ok').length,notOk:answers.filter(a=>a.status==='not_ok').length,na:answers.filter(a=>a.status==='na').length,evidence:evidences.length},answers}}
}
